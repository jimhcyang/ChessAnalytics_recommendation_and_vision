from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Chess board state detection and move recommendation system',
    author='Alpha Vision + Team',
    license='MIT',
)
